export const ATTENDANCE_STATUS = Object.freeze({
  DRAFT: 0,
  NOT_REVIEWED: 1,
  ATTENDANCE_ACCEPT: 2,
  ATTENDANCE_REJECT: 3,
})
export const MANAGE_TYPE = Object.freeze({
  CAN_REVIEW: 1,
  ONLY_VIEW: 2,
})
export const PERIOD = Object.freeze({
  TODAY: 1,
  THIS_WEEK: 2,
  THIS_MONTH: 3,
  ALL: 4,
})
export const BO_CONFIRM = Object.freeze({
  ACCEPT: 0,
  REJECT: 1,
})
export const ATTENDANCE_IMG_PATH = 'http://127.0.0.1:8000/storage/'
